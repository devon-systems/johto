apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy
  namespace: flux-system
spec:
  chart:
    spec:
      chart: alloy
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
      version: 1.12.1
  install:
    createNamespace: true
    remediation:
      retries: 3
  interval: 30m
  releaseName: alloy
  targetNamespace: alloy-system
  upgrade:
    remediation:
      retries: 3
  values:
    alloy:
      configMap:
        content: |-
          discovery.kubernetes "nodes" {
            role = "node"
          }

          discovery.relabel "cadvisor" {
            targets = discovery.kubernetes.nodes.targets
            rule {
              source_labels = ["__meta_kubernetes_node_name"]
              target_label = "node"
            }
          }

          prometheus.scrape "cadvisor" {
            targets = discovery.relabel.cadvisor.output
            job_name = "cadvisor"
            scheme = "https"
            metrics_path = "/metrics/cadvisor"
            scrape_interval = "30s"
            authorization {
              credentials_file = "/var/run/secrets/kubernetes.io/serviceaccount/token"
            }
            tls_config {
              ca_file = "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
            }
            forward_to = [prometheus.relabel.cadvisor.receiver]
          }

          prometheus.relabel "cadvisor" {
            forward_to = [prometheus.remote_write.johto.receiver]
            rule {
              source_labels = ["__name__"]
              regex = "up|scrape_.*|container_cpu_usage_seconds_total|container_cpu_cfs_periods_total|container_cpu_cfs_throttled_periods_total|container_memory_working_set_bytes|container_memory_rss|container_oom_events_total"
              action = "keep"
            }
          }

          prometheus.scrape "kube_state_metrics" {
            targets = [{__address__ = "kube-state-metrics.alloy-system.svc:8080"}]
            job_name = "kube-state-metrics"
            scrape_interval = "30s"
            forward_to = [prometheus.remote_write.johto.receiver]
          }

          discovery.kubernetes "postgres" {
            role = "pod"
            namespaces {
              names = ["cnpg-system"]
            }
            selectors {
              role = "pod"
              label = "cnpg.io/cluster=pg-shared"
            }
          }

          discovery.relabel "postgres" {
            targets = discovery.kubernetes.postgres.targets
            rule {
              source_labels = ["__meta_kubernetes_pod_container_port_number"]
              regex = "9187"
              action = "keep"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_phase"]
              regex = "Running"
              action = "keep"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_name"]
              target_label = "pod"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_node_name"]
              target_label = "node"
            }
            rule {
              source_labels = ["__meta_kubernetes_namespace"]
              target_label = "namespace"
            }
          }

          prometheus.scrape "postgres" {
            targets = discovery.relabel.postgres.output
            job_name = "pg-shared"
            scrape_interval = "30s"
            forward_to = [prometheus.relabel.postgres.receiver]
          }

          prometheus.relabel "postgres" {
            forward_to = [prometheus.remote_write.johto.receiver]
            rule {
              source_labels = ["__name__"]
              regex = "up|scrape_.*|cnpg_collector_up|cnpg_collector_last_collection_error|cnpg_pg_replication_in_recovery|cnpg_pg_replication_lag|cnpg_pg_replication_streaming_replicas|cnpg_pg_replication_is_wal_receiver_up|cnpg_backends_total|cnpg_backends_waiting_total|cnpg_pg_stat_database_(xact_commit|xact_rollback|deadlocks)|cnpg_pg_locks_count|cnpg_pg_database_size_bytes|cnpg_pg_stat_archiver_(archived_count|failed_count|last_archived_time)|barman_cloud_cloudnative_pg_io_(last_failed_backup_timestamp|last_available_backup_timestamp|first_recoverability_point)"
              action = "keep"
            }
          }

          discovery.kubernetes "flux" {
            role = "pod"
            namespaces {
              names = ["flux-system"]
            }
          }
          discovery.relabel "flux" {
            targets = discovery.kubernetes.flux.targets
            rule {
              source_labels = ["__meta_kubernetes_pod_container_port_name"]
              regex = "http-prom"
              action = "keep"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_phase"]
              regex = "Running"
              action = "keep"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_name"]
              target_label = "pod"
            }
          }
          prometheus.scrape "flux" {
            targets = discovery.relabel.flux.output
            job_name = "flux"
            scrape_interval = "30s"
            forward_to = [prometheus.relabel.flux.receiver]
          }
          prometheus.relabel "flux" {
            forward_to = [prometheus.remote_write.johto.receiver]
            rule {
              source_labels = ["__name__"]
              regex = "up|scrape_.*|gotk_reconcile_.*|controller_runtime_reconcile_.*|workqueue_.*"
              action = "keep"
            }
          }

          prometheus.exporter.postgres "immich" {
            data_source_names = ["postgresql://alloy_monitor:" + sys.env("IMMICH_MONITOR_PASSWORD") + "@postgres.immich.svc:5432/immich?sslmode=disable"]
          }
          prometheus.scrape "immich_postgres" {
            targets = prometheus.exporter.postgres.immich.targets
            job_name = "immich-postgres"
            scrape_interval = "30s"
            forward_to = [prometheus.relabel.immich_postgres.receiver]
          }
          prometheus.relabel "immich_postgres" {
            forward_to = [prometheus.remote_write.johto.receiver]
            rule {
              source_labels = ["__name__"]
              regex = "up|scrape_.*|pg_up|pg_exporter_last_scrape_error|pg_stat_database_.*|pg_database_size_bytes|pg_locks_count"
              action = "keep"
            }
          }

          prometheus.remote_write "johto" {
            endpoint {
              url = "http://10.254.2.3:3020/api/v1/write"
            }
          }

          discovery.kubernetes "traefik" {
            role = "pod"

            namespaces {
              names = ["kube-system"]
            }

            selectors {
              role  = "pod"
              label = "app.kubernetes.io/name=traefik"
            }
          }

          discovery.relabel "traefik" {
            targets = discovery.kubernetes.traefik.targets

            rule {
              source_labels = ["__meta_kubernetes_namespace"]
              target_label  = "namespace"
            }

            rule {
              source_labels = ["__meta_kubernetes_pod_name"]
              target_label  = "pod"
            }

            rule {
              source_labels = ["__meta_kubernetes_pod_container_name"]
              target_label  = "container"
            }

            rule {
              source_labels = ["__meta_kubernetes_namespace", "__meta_kubernetes_pod_container_name"]
              separator     = "/"
              target_label  = "job"
            }
          }

          loki.source.kubernetes "traefik" {
            targets    = discovery.relabel.traefik.output
            forward_to = [loki.process.traefik.receiver]
          }

          loki.process "traefik" {
            stage.static_labels {
              values = {
                cluster = "johto",
              }
            }

            forward_to = [loki.write.johto.receiver]
          }

          loki.write "johto" {
            endpoint {
              url = "http://10.254.2.3:3030/loki/api/v1/push"
            }
          }
      extraEnv:
        - name: IMMICH_MONITOR_PASSWORD
          valueFrom:
            secretKeyRef:
              name: immich-monitor
              key: password
      enableReporting: false
      resources:
        limits:
          cpu: 300m
          memory: 512Mi
        requests:
          cpu: 25m
          memory: 128Mi
    controller:
      type: deployment
      replicas: 1
    crds:
      create: false
    image:
      tag: v1.19.2
      digest: sha256:b8ec653c44235fbe910879145dac3597d66b0aaecf60bcbbe82580767771a839
