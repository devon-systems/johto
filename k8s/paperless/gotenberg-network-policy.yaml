apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: gotenberg
  namespace: paperless
spec:
  podSelector:
    matchLabels:
      app: gotenberg
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: paperless
      ports:
        - protocol: TCP
          port: 3000
  egress: []
