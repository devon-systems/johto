{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests",
    ":dependencyDashboard",
    "helpers:pinGitHubActionDigests"
  ],
  "labels": ["dependencies"],
  "timezone": "America/New_York",
  "platformAutomerge": true,

  "nix": {
    "enabled": true
  },

  "kubernetes": {
    "managerFilePatterns": ["/k8s/.+\\.ya?ml$/"]
  },

  "flux": {
    "managerFilePatterns": ["/k8s/.+\\.ya?ml$/"]
  },

  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "schedule": ["before 4am"]
  },

  "customManagers": [
    {
      "description": "Pinned images in local Helm chart templates",
      "customType": "regex",
      "managerFilePatterns": ["/^k8s/charts/.+/templates/.+\\.ya?ml$/"],
      "matchStrings": [
        "\\s*image:\\s+[\"']?(?<depName>[^\\s\"'{}]+?):(?<currentValue>[^\\s\"'@{}]+)@(?<currentDigest>sha256:[a-f0-9]{64})[\"']?"
      ],
      "datasourceTemplate": "docker"
    }
  ],

  "packageRules": [
    {
      "description": "Jellyfin image updates are managed in kustomization.yaml",
      "matchManagers": ["kubernetes"],
      "matchFileNames": ["k8s/jellyfin/jellyfin.yaml"],
      "matchPackageNames": ["jellyfin/jellyfin"],
      "enabled": false
    },
    {
      "description": "OCI Helm charts use chart versions, not container digests",
      "matchManagers": ["flux"],
      "pinDigests": false
    },
    {
      "description": "Auto-merge routine updates after CI passes",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin", "pinDigest"],
      "automerge": true
    },
    {
      "description": "Major bumps stay manual",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "Lock file maintenance updates FlakeHub inputs",
      "matchManagers": ["nix"],
      "matchPackageNames": ["/^https:\\/\\/flakehub\\.com\\/f\\//"],
      "enabled": false
    }
  ],

  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  }
}
